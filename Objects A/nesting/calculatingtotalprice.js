/* Create a function named calculateTotalPrice.
This function should take an array of objects as its parameter.
Each object in the array represents an item, and each item has a price property.
The function should calculate the total price of all items in the array by summing
up their individual prices and return the total sum. */

function calulateTotalPrice(obj) {
    return calculateTotalPrice(obj.price + obj.price)

}